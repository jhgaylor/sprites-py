#!/usr/bin/env python3
"""Wrapper script for the Sprites Python SDK test CLI."""

import os
import sys

# Add the src directory to the path
sys.path.insert(0, os.path.join(os.path.dirname(__file__), "src"))

from test_cli.main import main

if __name__ == "__main__":
    main()
